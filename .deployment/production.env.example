# Production Environment Variables Template
# Copy to server: /var/www/vk-retry/.env.production
# Fill in actual values (DO NOT commit real values to git!)

# Application environment
NODE_ENV=production

# Database connection
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://vk_user:STRONG_RANDOM_PASSWORD@localhost:5432/vk_production"

# NextAuth.js configuration
NEXTAUTH_URL="https://vk.retry.sk"

# NextAuth secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="GENERATE_RANDOM_SECRET_WITH_openssl_rand_base64_32"

# Application port (used by PM2)
PORT=3000

# Optional: Email configuration (if using email features)
# EMAIL_SERVER="smtp://username:password@smtp.example.com:587"
# EMAIL_FROM="noreply@vk.retry.sk"

# Optional: File upload limits
# MAX_FILE_SIZE=10485760  # 10MB in bytes

# Optional: Session configuration
# SESSION_MAX_AGE=2592000  # 30 days in seconds

# Optional: Logging level
# LOG_LEVEL="info"  # error | warn | info | debug

# Optional: Feature flags
# ENABLE_REGISTRATION=false
# MAINTENANCE_MODE=false
